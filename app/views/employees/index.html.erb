<head>
  <title>Students</title>
  <style>
    .title{
        grid-column: 2/3;
        grid-row: 1;
        border: 1px solid black;
        text-align: center;
        font-size: 48px;
    }
    
    .wrapper{
      display: grid; 
      grid-template-columns: 1fr 1fr 1fr;
      grid-auto-rows: minmax(100, auto);
      grid-gap: 2em;
      justify-items: stretch;
      align-items: stretch;
    }
    .wrapper div{
        background-color: #eee;
        padding: 1em;
    }
    .wrapper > div:nth-child(odd){
        background: #ddd;
    }
    .nested{
      display: grid;
      grid-template-columns:repeat(3, 1fr);
      grid-row: 2;
      grid-column: 2;
      grid-gap: 2em;
      justify-items: stretch;
      align-items: stretch;
    }
    .nested > div {
      border: #333 1px solid;
      background: lightgreen;
    }
  </style>
</head>
<body>
<div class = "wrapper">
  <div class = "title"><h1>Employees</h1></div>
  <div class = "nested" >
    <% @employees.each do |employee| %>
      <div>
        <li><a class="button" href="/employees/<%=employee.id%>"><%=employee.first_name%></a></li><br>
      </div>
    <% end %>
</div>
</body>












